numeros <- c(1, 2, 3, 4, 5)

#operações

soma = sum(numeros)     # soma do vetor
media = mean(numeros)    # média do vetor
tamanho = length(numeros)  # tamanho do vetor

numero_da_segunda_posicao_do_vetor = numeros[2]       # Acessar Posição

print(soma)
print(media)
print(tamanho)
print(numero_da_segunda_posicao_do_vetor)